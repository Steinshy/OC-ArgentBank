/**
 * Central export point for all application constants
 */

// API configuration and endpoints
export { API_BASE_URL, API_CONFIG, API_ENDPOINTS } from './api';

// Route paths
export { ROUTES, buildTransactionsRoute } from './routes';

// Storage keys
export { STORAGE_KEYS } from './storage';

// UI text and labels
export { APP, BUTTONS, MESSAGES, NAVIGATION, FORMS, TRANSACTION_CATEGORIES, TRANSACTION_TYPES, getCopyrightText, HOME_PAGE } from './ui';

// Transaction and styling constants
export { TABLE_STYLES, BUTTON_STYLES } from './transactions';
