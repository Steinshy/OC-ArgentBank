/** Matches `Backend/services/userService.js` login `throw new Error(...)` messages. */
export const SIGN_IN_USER_NOT_FOUND = 'User not found!';
export const SIGN_IN_PASSWORD_INVALID = 'Password is invalid';
